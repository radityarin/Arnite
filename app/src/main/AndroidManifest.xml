<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.go.arnite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.user.main.MainUserActivity"></activity>
        <activity android:name=".ui.user.signup.SignUpUserActivity">
        </activity>
        <activity android:name=".ui.mentor.signup.SignUpMentorActivity" />
        <activity android:name=".ui.common.forgotpassword.ForgotPasswordActivity" />
        <activity android:name=".ui.common.onboarding.OnBoardingScreen" />
        <activity android:name=".ui.common.signin.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.common.termscondition.TermsConditionActivity" />
        <activity android:name=".ui.common.splashscreen.SplashScreenActivity" />
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>